@using EntityLayer_HizmetPortal.Concrate
@model List<Galeri>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/UygulamaLayout.cshtml";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Responsive Image Gallery</title>
    <link rel="stylesheet" href="~/Css/StyleSheet2.css">
</head>
<body>
    <div class="gallery">
        <h2>📷 Galeri</h2>
        <div class="gallery-grid">
            @foreach (var image in Model)
            {
                if (image.GaleriStatus)
                {
                    <div class="gallery-item"><img src="@image.GaleriImage" alt="Gallery Image"></div>
                }
            }
        </div>

        <div class="popup-image" style="display: none;">
            <span>&times;</span>
            <img src="" alt="Popup Image" />
        </div>
    </div>

    <script>
        document.querySelectorAll('.gallery-grid img').forEach(image => {
            image.onclick = () => {
                const popup = document.querySelector('.popup-image');
                popup.style.display = 'flex';
                popup.querySelector('img').src = image.getAttribute('src');
            }
        });

        document.querySelector('.popup-image span').onclick = () => {
            document.querySelector('.popup-image').style.display = 'none';
        }
    </script>
</body>
</html>
